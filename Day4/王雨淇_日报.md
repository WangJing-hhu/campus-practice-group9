# 王雨淇 Day4 个人日报

**姓名**：王雨淇
**日期**：2026-07-17
**角色**：Java 后端——文档数据层

---

## 今日工作

### 1. kb_document 数据库表设计
- 编写 `sql/day4_kb_document.sql`，严格按照 Day4 文档的字段定义
- 包含 id, title, original_name, stored_name, file_path, file_type, file_size, chunk_count, status, process_stage, error_message, create_user_id, create_time, update_time, deleted 共 15 个字段
- 添加 status、file_type、create_user_id、create_time、deleted 索引
- 状态约束：PENDING/PROCESSING/COMPLETED/FAILED

### 2. KbDocument 实体与 Mapper
- 编写 KbDocument.java，继承昨日 User.java 的代码风格
- 使用 @TableLogic 逻辑删除、@TableField fill 自动填充时间
- 编写 KbDocumentMapper.java，继承 BaseMapper<KbDocument>

### 3. DTO/VO 层
- DocumentQueryDTO：page, size, keyword, status 四个查询参数
- DocumentUpdateDTO：title 可选更新字段
- DocumentVO：安全视图，隐藏 filePath 等内部字段

### 4. FileStorageUtil 文件工具
- UUID 重命名防冲突
- 格式校验：pdf/doc/docx/txt
- 大小限制：50MB
- 路径穿越防护
- save / replace / delete 三操作

### 5. AsyncConfig 线程池配置
- docProcessExecutor Bean：core=2, max=4, queue=50
- CallerRunsPolicy 拒绝策略
- @EnableAsync 开启异步支持

### 6. WebClientConfig
- aiWebClient Bean：baseUrl=http://localhost:8000
- 连接超时 5s，响应超时 120s
- X-Internal-Token header 防外部伪造

### 7. pom.xml 与 application.yml 更新
- pom.xml：新增 spring-boot-starter-webflux、reactor-test
- application.yml：新增 servlet.multipart 50MB、campus-qa 自定义配置段

## 困难与解决过程

1. **GitHub 网络问题**：本机无法通过 git 协议连接 GitHub（443 端口被重置），但 `gh api` 可用。通过 GitHub CLI 的 API 获取仓库代码结构和关键文件内容，完成代码编写。代码文件在本地 Desktop 准备好，等网络恢复后推送。

2. **WebFlux 与 WebMVC 共存**：需要 WebClient 但不想引入 WebFlux 框架替代 MVC。确认 Spring Boot 3 在同时存在两个 starter 时默认使用 MVC 自动配置，WebClient 仅作为 HTTP 客户端使用，互不影响。

3. **文件路径一致性**：Python 和 Java 需要访问同一文件。采用绝对路径保存策略，两端通过 `storage/uploads` 共享目录读取。

## 明日计划

- 配合杨熙杰联调数据层：确认分页查询、字段映射、文件路径
- 配合陆奥琪确认 AI 服务 callback 接口
- 参加全链路联调（上传 → 处理 → 列表 → 搜索 → 删除）
- 完成关键截图

## PR 信息

- 分支：`feat/day4-backend-doc-data`
- PR 目标：`dev/day4`
- 审查者：杨熙杰
- 审查重点：字段、配置、文件安全、MyBatis-Plus 映射
